- name: template
  template:
    src: pipeline.xml
    dest: /tmp/pipeline.xml
  loop:
    - { repoName: "cart", componentName: "cart" }

- name: Create a jenkins job by using the token
  community.general.jenkins_job:
    config: "{{ lookup('templates', 'templates/pipeline.xml') }}"
    name: test
    url: http://172.31.4.194:8080
    user: "admin"
    token: "11edc4b20bcb8999dbaaecd36780af73cf"
